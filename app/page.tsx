export default function Home() {
  return (
    <main style={{ padding: 40 }}>
      <h1>TMO Inteligente — Servidor de licencias</h1>
      <p>Este servicio no tiene interfaz propia — expone /api/activar y /api/validar para la app de escritorio.</p>
    </main>
  );
}
